{
  "name": "local-tests-pw",
  "version": "1.0.0",
  "description": "Playwright ene-to-end tests for localcoding.us",
  "scripts": {
    "test": "playwright test",
    "tets:debug": "playwright test --headed",
    "report": "playwright show-report",
    "format": "prettier ./ --write --list-different",
    "lint": "prettier ./ --check"
  },
  "keywords": [],
  "author": "Jalalov",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "1.37.1",
    "prettier": "3.0.3"
  },
  "dependencies": {
    "dotenv": "16.3.1"
  }
}
